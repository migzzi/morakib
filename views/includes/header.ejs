<div class="main-header mb-4">
    <div class="logo-header">
        <a href="index.html" class="logo">
            نظـــام المخالفـــات  
        </a>	<i class="la la-2x la-map-marker"></i>
        <button class="navbar-toggler sidenav-toggler ml-auto" type="button" data-toggle="collapse" data-target="collapse" aria-controls="sidebar" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <button class="topbar-toggler more"><i class="la la-ellipsis-v"></i></button>
    </div>
    <nav class="navbar navbar-header navbar-expand-lg">
        <div class="container-fluid">
            


                <li class="nav-item dropdown">
                    <a class="dropdown-toggle profile-pic" data-toggle="dropdown" href="#" aria-expanded="false"> <img src="/img/avatars/<%= user.avatar %>" alt="user-img" width="36" class="img-circle"><span ><%=user.first_name %> <%= user.last_name %></span></span> </a>
                    <ul class="dropdown-menu dropdown-user">
                        <li>
                            <div class="user-box">
                                <div class="u-img"><img src="/img/avatars/<%= user.avatar %>" alt="user"></div>
                                <div class="u-text">
                                    <h4><%=user.first_name %> <%= user.last_name %></h4>
                                    <p class="text-muted"><%= user.email%></p>
                                    <a href="/profile/<%= user.username %>" class="mb-2 btn btn-rounded btn-danger btn-sm">البيانات الشخصية</a>
                                    <a href="/profile/<%= user.username %>/edit" class="btn btn-rounded btn-danger btn-sm">تعديل البيانات</a>
                                </div>
                            </div>
                        </li>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item logout-btn" href="#"><i class="fa fa-power-off"></i> تسجيل خروج</a>
                    </ul>
                        <!-- /.dropdown-user -->
                    </li>
                </ul>
            </div>
        </nav>
    </div>
